{
  "name": "ts-auto-rename",
  "version": "0.0.1",
  "author": {
    "name": "Kryštof Řezáč",
    "url": "https://github.com/krystofrezac"
  },
  "bin": {
    "ts-auto-rename": "./bin/ts-auto-rename"
  },
  "files": [
    "dist",
    "bin"
  ],
  "scripts": {
    "build": "rimraf dist && tsc"
  },
  "devDependencies": {
    "@types/node": "^20.3.3",
    "rimraf": "^5.0.1",
    "typescript": "^5.1.6"
  }
}
